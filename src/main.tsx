import { createRoot } from "react-dom/client";
import App from "./App.tsx";
import "./index.sass";

createRoot(document.getElementById("root")!).render(<App />);

// px-[30px] flex items-center bg-white gap-4  h-[130px] shadow-custom box-border border-b border-b-[#F0F0F0]
